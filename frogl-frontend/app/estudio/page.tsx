import { EstudioApp } from "@/components/pitch/EstudioApp";

export const metadata = {
  title: "Estudio — FROGL",
  description: "Cámara y traductor de texto, sin jurado.",
};

export default function EstudioPage() {
  return <EstudioApp />;
}
