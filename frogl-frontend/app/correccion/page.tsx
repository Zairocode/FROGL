import { ReviewRoom } from "@/components/review/ReviewRoom";

export const metadata = {
  title: "Corrección — FROGL",
  description: "Lo que hay que arreglar antes de que te evalúe el jurado.",
};

export default function CorreccionPage() {
  return <ReviewRoom />;
}
