# Desarrollo local (Linux Mint, Node nativo). Copiar a `.env` y ajustar.
# Railway inyecta PORT y el resto de variables en el dashboard; no usar este archivo en producción.

NODE_ENV=development
PORT=3000
HOST=0.0.0.0
